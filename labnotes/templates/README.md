# Lab Notes publishing

Lab Notes now use a single source file: `Resources/labnotes.js`.

A commented copy/paste template is kept at the top of that file. Add the new entry to the `notes` array, commit, and push. No separate post HTML file or feed entry is required.
